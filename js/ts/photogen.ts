class photogen {
    private image: string;
    private text: string;
    private imgBackground: any;
    private imgMain: any;
    private imgText: any;
    private textBackground: any;

    constructor(image: string, text: string) {
        this.text = text;
        this.image = image;
    }

    // create background (image)

    // create smaller image (square image)

    // create text

    // create text background

    // build image
        // add logo (static)
}